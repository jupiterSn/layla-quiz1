open terminal in place where I want my file to be 
npm create vite@latest
and continue process
open vs code terminal 
   git init 
   git add .
   git commit -m "first commit"
   create a new repository 
   git remote add origin with link 
   git branch -M main
   push -U origin main

   to clone repository from github to vs code 
   we take the link 
   typre in terminal git clone plus link 
   then cd name project and open it 
